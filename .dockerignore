# 이미지에 넣지 않는 것.
#
# 특히 iopaint/tests 를 빼는 이유가 크다. 테스트가 이미지에 들어가면 테스트만
# 고쳐도 digest 가 바뀌고, CI 가 그 digest 를 커밋하면 Argo 가 selfHeal 로
# 즉시 따라가 파드를 재생성한다 - 런타임이 하나도 안 바뀐 배포다.
# 실제로 두 번 그렇게 됐고, 워크플로 경로 필터로는 못 막았다(워크플로 파일
# 자체를 고치면 또 걸린다). 원인은 트리거가 아니라 이미지 내용이다.
iopaint/tests/

# 개발 부산물
.venv/
.git/
.github/
**/__pycache__/
*.pyc
*.egg-info/
deploy/
scripts/
*.md
!README.md
